<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ================================================= -->
    <!--           PERMISSÕES DO AGRO AUDIT RJ             -->
    <!-- ================================================= -->

    <!-- Hardware e Sensores (Câmera e GPS) -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    
    <!-- Internet (Para Mapas e Waze) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Armazenamento (Celulares Antigos - Android 12 ou menos) -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

    <!-- Armazenamento (Celulares Novos - Android 13+ / Fotos do Drone) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

    <!-- ================================================= -->
    <!--              CONFIGURAÇÃO DO APP                  -->
    <!-- ================================================= -->

    <application
        android:label="Agro Audit RJ"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- ============================================================== -->
        <!--   👇👇👇 ÁREA PARA ADICIONAR NOVAS CONFIGURAÇÕES 👇👇👇        -->
        <!--   Coloque chaves de API, AdMob ou Firebase aqui abaixo         -->
        <!-- ============================================================== -->

        <!-- CHAVE DO GOOGLE MAPS (Obrigatório para o mapa aparecer) -->
        <!-- Substitua o valor abaixo pela sua chave real quando tiver uma -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="SUA_CHAVE_GOOGLE_AQUI" />

    </application>
    
    <!-- Configuração para o URL Launcher (Abrir Waze/Maps) funcionar -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Permite abrir o Google Maps -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="google.navigation" />
        </intent>
        <!-- Permite abrir o Waze -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="waze" />
        </intent>
    </queries>
</manifest>